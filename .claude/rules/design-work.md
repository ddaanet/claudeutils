---
paths:
  - "plans/*/design.md"
  - "plans/*/runbook.md"
---

# Design Work Context

**Design decision escalation:** When facing architectural choices between approaches, use `/opus-design-question` to consult Opus instead of asking the user via AskUserQuestion.

Only ask the user for: subjective preferences, business logic, scope changes, or genuinely unclear requirements.
