---
paths:
  - "src/cli/**"
  - "bin/**"
---

When working on CLI-related files, read `agents/decisions/cli.md` for established patterns and conventions.

Key areas: Error output patterns, output formats (text/JSON), entry point configuration
