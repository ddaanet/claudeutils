---
paths:
  - ".claude/agents/**/*"
---

# Agent Development Context

**Before editing agent files, load the agent development guide:**

```
/plugin-dev:agent-development
```

This provides agent structure, frontmatter fields, system prompt patterns, triggering conditions, and tool access configuration.
