---
paths:
  - ".claude/commands/**/*"
---

# Command Development Context

**Before editing command files, load the command development guide:**

```
/plugin-dev:command-development
```

This provides command structure, YAML frontmatter, argument patterns, bash execution context, and user interaction with AskUserQuestion.
