# Session: Worktree — Block index.lock rm

**Status:** Focused worktree for parallel execution.

## Pending Tasks

- [ ] **Block index.lock rm** — PreToolUse hook blocking `rm */index.lock`, says retry | haiku | restart
