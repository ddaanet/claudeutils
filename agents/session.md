# Session: Worktree — WT new sentinel copy

**Status:** Focused worktree for parallel execution.

## Pending Tasks

- [ ] **WT new sentinel copy** — Copy `tmp/.test-sentinel` during `_worktree new` | sonnet
