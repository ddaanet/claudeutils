# Recall Artifact: Command Lint Gate

Resolve entries via `claudeutils _recall resolve` — do not use inline summaries.

## Entry Keys

when designing quality gates — layered defense, commit chokepoint enforcement
when placing quality gates — commit chokepoint, scripted mechanical enforcement
when splitting validation into mechanical and semantic — script deterministic, agent judgment advisory
when choosing script vs agent judgment — non-cognitive deterministic scripting
when pending tasks lack recovery context — backtick command requirement origin
when constraining task names for slug validity — layers don't share constraints (precedent for validation scope)
when lint-gated recall needs error categorization — errorformat parsing reuse path
