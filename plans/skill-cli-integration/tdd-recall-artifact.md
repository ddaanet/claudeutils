# TDD Recall Artifact: Skill-CLI Integration

Resolve ALL entries via `claudeutils _recall resolve` before starting work.

## Entry Keys

when writing hook redirect messages — include rationale in hook output messages
