# Recall Artifact: Remove Fuzzy Recall

Resolve entries via `claudeutils _recall resolve` — do not use inline summaries.

## Entry Keys

when cli error messages are llm-consumed — error message design (facts only, no suggestions)
when writing memory-index trigger phrases — exact matching design intent, article alignment
when grounding recall system behavior — fuzzy matcher prefix-noise tolerance as known issue
when design proposes removing functions — corrector must verify all callers and purposes
when testing CLI tools — Click CliRunner for in-process testing
when triaging behavioral code changes as simple — Moderate minimum for behavioral code
