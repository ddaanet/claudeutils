# Recall Artifact: Fix Planstate Detector

Resolve entries via `agent-core/bin/when-resolve.py` — do not use inline summaries.

## Entry Keys

when adding a new variant to an enumerated system — grep downstream enumeration sites after updating authoritative definition
when checking expected program state — boolean return not exception, EAFP state-query
when choosing script vs agent judgment — deterministic operations should be scripted
