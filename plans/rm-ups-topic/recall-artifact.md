# Recall Artifact: Remove UPS Topic Injection

Resolve entries via `claudeutils _recall resolve` — do not use inline summaries.

## Entry Keys

how filter user prompt submit hooks — UPS hook filtering pattern (script-internal)
when mapping hook output channel audiences — additionalContext vs systemMessage channel semantics
when evaluating recall system effectiveness — motivation for topic injection (recognition vs retrieval gap)
