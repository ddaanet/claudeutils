# Recall Artifact: Prose Infrastructure Batch

Resolve entries via `claudeutils _recall resolve` — do not use inline summaries.

## Entry Keys

when editing skill files — skill frontmatter conventions, description dual-purpose, control flow verification
when placing DO NOT rules in skills — negative constraints co-located with positive guidance
when designing quality gates — layered defense, multiple independent checks
when placing quality gates — commit chokepoint, scripted mechanical enforcement
when splitting validation into mechanical and semantic — script deterministic, agent judgment advisory
when adding a new variant to an enumerated system — grep downstream enumeration sites
when testing CLI tools — Click CliRunner, in-process, isolated filesystem
when loading context before skill edits — plugin-dev skill-development before editing
when choosing script vs agent judgment — non-cognitive deterministic script it
when triaging behavioral code changes as simple — structural criteria moderate minimum
when selecting model for prose artifact edits — opus for skills fragments agents
