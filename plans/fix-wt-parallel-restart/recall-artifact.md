# Recall Artifact: Fix wt parallel restart

Resolve entries via `claudeutils _recall resolve` — do not use inline summaries.

## Entry Keys

when tracking worktree tasks in session.md — inline marker pattern, section rendering
when partitioning work for parallel agents — fire-and-forget, scope partition
when custom agents need session restart for discoverability — restart semantics
when constraining task names for slug validity — layer separation principle
