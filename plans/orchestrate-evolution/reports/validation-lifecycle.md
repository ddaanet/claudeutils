# Validation Report: lifecycle

**Runbook:** plans/orchestrate-evolution/

**Date:** 2026-02-24T11:43:53Z

**Result:** FAIL

## Summary

Failed: 12

## Violations

- Cycle 1.1: `agent-core/bin/prepare-runbook.py` modified before creation (first seen in Cycle 1.1 as 'Replace `generate_phase_agent` with `generate_task_agent` that takes all plan context')
- Cycle 1.2: `agent-core/bin/prepare-runbook.py` modified before creation (first seen in Cycle 1.1 as 'Replace `generate_phase_agent` with `generate_task_agent` that takes all plan context')
- Cycle 1.2: `agent-core/bin/prepare-runbook.py` modified before creation (first seen in Cycle 1.1 as 'Replace `generate_phase_agent` with `generate_task_agent` that takes all plan context')
- Cycle 1.3: `agent-core/bin/prepare-runbook.py` modified before creation (first seen in Cycle 1.1 as 'Replace `generate_phase_agent` with `generate_task_agent` that takes all plan context')
- Cycle 1.4: `agent-core/bin/prepare-runbook.py` modified before creation (first seen in Cycle 1.1 as 'Replace `generate_phase_agent` with `generate_task_agent` that takes all plan context')
- Cycle 1.4: `agent-core/bin/prepare-runbook.py` modified before creation (first seen in Cycle 1.1 as 'Replace `generate_phase_agent` with `generate_task_agent` that takes all plan context')
- Cycle 1.4: `agent-core/bin/prepare-runbook.py` modified before creation (first seen in Cycle 1.1 as 'Replace `generate_phase_agent` with `generate_task_agent` that takes all plan context')
- Cycle 2.2: `agent-core/bin/prepare-runbook.py` modified before creation (first seen in Cycle 1.1 as 'Replace `generate_phase_agent` with `generate_task_agent` that takes all plan context')
- Cycle 2.3: `agent-core/bin/prepare-runbook.py` modified before creation (first seen in Cycle 1.1 as 'Replace `generate_phase_agent` with `generate_task_agent` that takes all plan context')
- Cycle 3.1: `agent-core/bin/prepare-runbook.py` modified before creation (first seen in Cycle 1.1 as 'Replace `generate_phase_agent` with `generate_task_agent` that takes all plan context')
- Cycle 3.2: `agent-core/bin/prepare-runbook.py` modified before creation (first seen in Cycle 1.1 as 'Replace `generate_phase_agent` with `generate_task_agent` that takes all plan context')
- Cycle 3.2: `agent-core/bin/prepare-runbook.py` modified before creation (first seen in Cycle 1.1 as 'Replace `generate_phase_agent` with `generate_task_agent` that takes all plan context')
