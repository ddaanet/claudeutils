# Recall Artifact: Pipeline Review Protocol

Resolve entries via `claudeutils _recall resolve` — do not use inline summaries.

## Entry Keys

when d: mode validates a proposed change — discussion → p: chain, captures decisions as tasks
how wrap a discussion session — w command crystallizes findings into persistent artifacts
when capturing requirements from conversation — extract mode primary, elicit secondary
when implementation modifies pipeline skills — inline task sequence, not Tier 3; self-modification coupling
when bootstrapping self-referential improvements — apply improvement before using tool in subsequent steps
when choosing review gate — transformation table T1-T6.5, maps artifact type to corrector
when reviewing expanded phases — LLM failure mode checks at BOTH outline AND expanded levels
when corrector agents lack recall mechanism — every corrector needs recall via self-contained, caller-injected, or skill-level
when deliverable review catches drift — inter-file consistency gaps per-step vet misses
when scoping corrector on TDD deliverables — full branch diff main HEAD not last cycle
when execution routing preempts skill scanning — skill matching must precede execution routing
when design ceremony continues after uncertainty resolves — two gates: entry + mid-stream re-check
when reviewing skill deliverable — skill-reviewer agent for cross-project context
when batch review routing overrides per-artifact judgment — proportionality per-file, route by artifact type
